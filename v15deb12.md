# Guía de Instalación de ERPNext Versión 15 en debian 12


## Prerrequisitos

### Requisitos de Software
- debian12 actualizado
- Un usuario con privilegios sudo
- Python 3.10+
- Node.js 18
``
### Requisitos de Hardware
- 4GB de RAM
- Disco duro de 40GB

## Configuración del Servidor

### Actualizar y Mejorar Paquetes
```sh
sudo apt-get update -y
sudo apt-get upgrade -y
```

### Crear un Nuevo Usuario (usuario de bench)
```sh
sudo adduser [frappe-user]
usermod -aG sudo [frappe-user]
su [frappe-user]
cd /home/[frappe-user]
```
Asegúrate de reemplazar `[frappe-user]` con tu nombre de usuario. Ejemplo: `sudo adduser frappe`.

### Instalar Paquetes Necesarios

#### Instalar GIT
```sh
sudo apt install -y git

```

#### Instalar Python
Debian 12 ya incluye Python 3.11, por lo que solo instalamos dependencias:
```sh
sudo apt install -y python3-dev python3-setuptools python3-pip python3-venv python3-distutils

```

#### Node.js 18 (repositorio oficial)
```sh
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

```

#### Redis
```sh
sudo apt install -y redis-server

```

#### wkhtmltopdf
```sh
sudo apt install -y xvfb libfontconfig wkhtmltopdf

```

#### Librerías adicionales
```sh
sudo apt install -y libmariadb-dev
apt-cache policy libmariadb-dev
```

#### Instalar MariaDB
```sh
sudo apt install mariadb-server mariadb-client
```

### Configurar el Servidor MYSQL

#### Configurar el Servidor
```sh
sudo mysql_secure_installation
```
Sigue las indicaciones para completar la configuración correctamente.
- Enter current password for root: (Enter your SSH root user password)
- Switch to unix_socket authentication [Y/n]: Y
- Change the root password? [Y/n]: Y
- Remove anonymous users? [Y/n] Y
- Disallow root login remotely? [Y/n]: N
- Remove test database and access to it? [Y/n]: Y
- Reload privilege tables now? [Y/n]: Y

#### Editar el Archivo de Configuración Predeterminado de MYSQL
```sh
sudo nano /etc/mysql/mariadb.conf.d/50-server.cnf

```
Añade el siguiente bloque de código:
```ini
[mysqld]
character-set-client-handshake = FALSE
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

[mysql]
default-character-set = utf8mb4
```

#### Reiniciar el Servidor MYSQL
```sh
sudo systemctl restart mariadb
```
```sh
sudo mysql -e "SHOW VARIABLES LIKE 'char%';"

```


#### Instalación de Node.js 18
```sh
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

```
```sh
node -v
npm -v

```

#### IInstalación del entorno Python moderno (pipx + uv)
```sh
sudo apt install -y pipx
pipx ensurepath
```

```sh
pipx install uv
```

#### Instalación de Bench usando pipx
```sh
pipx install frappe-bench
```
```sh
pipx ensurepath
```
```sh
source ~/.bashrc
```
```sh
bench --version
```
### nstalar yarn correctamente en Debian 12
```sh
sudo corepack enable

```
```sh
sudo corepack prepare yarn@1.22.19 --activate

```
```sh
yarn -v

```

### Crear el entorno Bench
### Inicializar Bench
```sh
bench init --frappe-branch version-15 frappe-bench
```
### Entrar al directorio:
```sh
cd frappe-bench

```

### Cambiar Permisos del Directorio del Usuario
```sh
chmod -R o+rx /home/[frappe-user]
```

### Crear un Nuevo Sitio
```sh
bench new-site [site-name]
```

### Instalar ERPNext y Otras Aplicaciones

#### Descargar Aplicaciones
```sh
bench get-app payments
bench get-app --branch version-15 erpnext
bench get-app --branch version-15 hrms

```

#### Instalar Aplicaciones en Nuestro Sitio
```sh
bench --site [site-name] install-app erpnext
bench --site [site-name] install-app hrms
```

#### cuando estas en desarrollo hay que hacer esto para entrar al login
```sh
bench use [nombre del sitio]
echo [nombre del sitio] > sites/currentsite.txt
cat sites/currentsite.txt
```

### Iniciar el Servidor
```sh
bench start
```
Visita `[TU IP DEL SERVIDOR:8000]` para ver ERPNext en funcionamiento.

### Configuración de ERPNext para Producción

#### Instalar Ansible globalmente para root
```sh
sudo apt update
sudo apt install ansible -y
```
#### Configurar Producción
```sh
sudo apt install -y supervisor nginx
```
```sh
sudo ln -s /home/corad/.local/bin/bench /usr/local/bin/bench
```
```sh
which bench
```
```sh
sudo bench setup production [frappe-user]
```
```sh
bench setup nginx
sudo systemctl restart nginx
```
```sh
sudo supervisorctl restart all
```

# Configuración de la Tenencia Múltiple en ERPNext
 
### 1. Configurar DNS para tenencia múltiple:
```sh
bench config dns_multitenant on
```

### 2. Crear un nuevo sitio:
```sh
bench new-site first.site
```
### 3. Configurar NGINX:
```sh
sudo service nginx reload
```
### 4. Instalar ERPNext en el nuevo sitio:
```sh
bench --site first.site install-app erpnext
```
#### 5. Asignar cada dominio a su sitio
```sh
bench setup add-domain dominio.com --site nombre_del_sitio

Ejemplos:

bench setup add-domain arps.erpcorad.xyz --site arps
bench setup add-domain adescop.erpcorad.xyz --site adescopa

#### 5. Asignar cada dominio a su sitio de forma manual o solo valida si el metodo anterior funciona

sudo nano sites/arps/site_config.json
"host_name": "arps.erpcorad.xyz"
sudo nano sites/adescopa/site_config.json
"host_name": "adescop.erpcorad.xyz"
```
```sh
bench setup nginx
config/nginx.conf
sudo mv config/nginx.conf /etc/nginx/conf.d/frappe-bench.conf
sudo chmod 644 /etc/nginx/conf.d/frappe-bench.conf
```

#### Insertar los bloques correctos para cada dominio
```sh
sudo nano /etc/nginx/conf.d/frappe-bench.conf
```

<details>
   <summary>Haz clic para expandir</summary>

# ------------------------------
# Upstreams
# ------------------------------

upstream frappe-bench-frappe {
    server 127.0.0.1:8000 fail_timeout=0;
}

upstream frappe-bench-socketio-server {
    server 127.0.0.1:9000 fail_timeout=0;
}

# ------------------------------
# Sitio ARPS
# ------------------------------

server {
    listen 80;
    listen [::]:80;
    server_name arps.erpcorad.xyz;

    root /home/corad/frappe-bench/sites;

    proxy_buffer_size 128k;
    proxy_buffers 4 256k;
    proxy_busy_buffers_size 256k;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "same-origin, strict-origin-when-cross-origin";

    location /assets {
        try_files $uri =404;
        add_header Cache-Control "max-age=31536000";
    }

    location /socket.io {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header X-Frappe-Site-Name arps;
        proxy_set_header Origin $scheme://$http_host;
        proxy_pass http://frappe-bench-socketio-server;
    }

    location / {
        try_files /arps/public/$uri @frappe;
    }

    location @frappe {
        proxy_set_header X-Frappe-Site-Name arps;
        proxy_set_header X-Use-X-Accel-Redirect True;
        proxy_read_timeout 120;
        proxy_redirect off;
        proxy_pass http://frappe-bench-frappe;
    }
}

# ------------------------------
# Sitio ADESCOPA
# ------------------------------

server {
    listen 80;
    listen [::]:80;
    server_name adescop.erpcorad.xyz;

    root /home/corad/frappe-bench/sites;

    proxy_buffer_size 128k;
    proxy_buffers 4 256k;
    proxy_busy_buffers_size 256k;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "same-origin, strict-origin-when-cross-origin";

    location /assets {
        try_files $uri =404;
        add_header Cache-Control "max-age=31536000";
    }

    location /socket.io {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header X-Frappe-Site-Name adescopa;
        proxy_set_header Origin $scheme://$http_host;
        proxy_pass http://frappe-bench-socketio-server;
    }

    location / {
        try_files /adescopa/public/$uri @frappe;
    }

    location @frappe {
        proxy_set_header X-Frappe-Site-Name adescopa;
        proxy_set_header X-Use-X-Accel-Redirect True;
        proxy_read_timeout 120;
        proxy_redirect off;
        proxy_pass http://frappe-bench-frappe;
    }
}
 
 
</details>


### Validar y reiniciar nginx

```sh
sudo nginx -t
sudo systemctl restart nginx
```
### Verificar funcionamiento de cada sitio
```sh
curl -I http://arps.erpcorad.xyz
curl -I http://adescop.erpcorad.xyz
```

#### Por favor, tenga en cuenta el nombre de sus sitios. Deberá agregar una ruta para cada sitio que agregue.
#### Para más detalles, puedes visitar el artículo original en [Code con Karani](https://codewithkarani.com/2021/08/24/setting-up-multi-tenancy-in-erpnext/).
---
 # **Instale y configure Certbot para SSL:**
```sh
sudo apt-get remove certbot
```
```sh
sudo apt install snapd
```
```sh
sudo snap install core
```
```sh
sudo snap refresh core
```
```sh
sudo snap install --classic certbot
```
```sh
sudo ln -s /snap/bin/certbot /usr/bin/certbot
```
```sh
sudo certbot --nginx
```
###Elimine los sitios nginx estándar. Para evitar conflictos y evitar vunlerabidades de seguridad  
```sh
sudo rm /etc/nginx/sites-available/default
sudo rm /etc/nginx/sites-enabled/default

```
### para registrar un certificado ssl al sitio nuevo solo este codigo
```sh
sudo certbot --nginx -d sitio.erpcorad.xyz

```

####**Una vez hecho esto, puede cerrar su cliente SSH, abrir un navegador y escribir la URL de su servidor en la barra de direcciones para acceder a la versión segura de su servidor ERPNext.**

####**Nota final:**
####    Este método específicamente NO cambia el nombre de banco o los directorios del sitio para brindar flexibilidad más adelante en caso de que necesite tener varias URL que apunten a la misma instancia de ERPNext o si necesita cambiar su URL por algún motivo en el futuro. Usar el `bench setup add-domain` comando hace que esto sea mucho más simple. Las instrucciones del certificado SSL proporcionadas están diseñadas para admitir esta configuración.

https://copilot.microsoft.com/shares/BHV2XHXvYjJB5SX1tiCph

#### Habilitar el Programador
```sh
bench --site [site-name] enable-scheduler
```

#### Desactivar el Modo de Mantenimiento
```sh
bench --site [site-name] set-maintenance-mode off
```


#### Habilita los script de cliente y servidor

```sh
bench set-config -g server_script_enabled 1
```
#### Habilita desarrollador
```sh
nano ~/frappe-bench/sites/common_site_config.json

"developer_mode": 1


```


